# Policy inference and training
torch>=2.0

# LeRobot (dataset format, ACT policy, training pipeline)
# Install from source for latest features:
#   git clone https://github.com/huggingface/lerobot.git
#   cd lerobot && pip install -e ".[all]"
# Or install from PyPI:
lerobot>=0.5.0

# SO-101 robot deployment (Feetech serial servos)
feetech-servo-sdk>=1.0.0

# Deployment (webcam capture and visualization; must NOT use opencv-python-headless)
opencv-python>=4.8
# If preview fails with "cv2.imshow ... not implemented", run:
#   pip uninstall opencv-python-headless && pip install opencv-python

# Pose estimation (keypoint mode)
mediapipe>=0.10

# YOLO pose estimation + segmentation (keypoint mode, yolo backend)
ultralytics>=8.3

# Evaluation plots
matplotlib>=3.7
numpy>=1.24
